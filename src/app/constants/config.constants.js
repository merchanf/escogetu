const MIN_DETAILED_RESTAURANTS = 10;
const RADIUS = 2500;
const FIRESTORE_PAGINATION_LIMIT = 6;
const FIRESTORE_MINIMUM_RESTAURANTS = 3;

const config = {
  MIN_DETAILED_RESTAURANTS,
  RADIUS,
  FIRESTORE_PAGINATION_LIMIT,
  FIRESTORE_MINIMUM_RESTAURANTS,
};

export default config;
